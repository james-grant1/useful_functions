def mean(this_list):
    '''
    Takes a list of number and returns the mean
    '''
    
    if len(this_list) <= 0:
        return "You passed an empty list"
    
    total=0
    for item in list:
        total = total + list
    return total/len(list)